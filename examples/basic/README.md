# Integration Example

This project verifies that `idlecore` can be consumed as a dependency from another package in the same repository and run as a small framework-driven game loop.

## Run

```bash
pnpm install
pnpm build
pnpm example:basic
```
