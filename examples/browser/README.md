# Browser Example

This example runs `idlecore` in the browser with an interactive UI.

It demonstrates:

- passive resource generation
- generator purchasing
- upgrade purchasing
- unlock progression
- achievement tracking
- fixed-step runtime scheduling
- offline simulation

## Run

```bash
pnpm install
pnpm build
pnpm example:browser
```

Then open the local Vite URL in your browser.
